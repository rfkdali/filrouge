#!/bin/sh

BRANCH=`git rev-parse --abbrev-ref HEAD`

git pull origin $BRANCH && \
git push origin $BRANCH && \

echo "Mise en ligne sur Heroku" && \
echo "git push heroku origin $BRANCH" && \
git push heroku origin $BRANCH
