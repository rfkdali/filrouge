<h1><span><%= link_to '+ Ajouter un objectif', new_goal_path, {:class => "add-btn btn"} %></span></h1>

<div id="goal_container">
	
	<%= render 'goals', locals: {:category => 'implémentation'} %>

	<!-- locals? -->

	<%= link_to 'retour', goals_path, {:class => "back-btn btn"}%>
</div>

